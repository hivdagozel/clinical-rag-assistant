[pytest]
markers =
    offline: Offline tests that do not require network.
    online: Online tests that require network access.
    integration: Local API and frontend integration tests.
testpaths = tests
python_files = test_*.py
